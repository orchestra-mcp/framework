# OCD-5: Employee Dashboard

**Type**: Epic | **Status**: backlog | **Priority**: high

Full employee dashboard with progress widgets (tasks, time, commits), synced task hierarchy (project->epic->story->task with markdown), timer/break/pomodoro reports with CSV/PDF export, version control integration (commits, PRs, issues), AI agent control panel (status, permissions, prompts via WebSocket), notifications with agent alerts, and sprint tracking with Jira/Linear/GitHub sync.
