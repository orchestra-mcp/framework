# OC-1: Build System Migration

**Type**: Epic | **Status**: backlog | **Priority**: high

Migrate pnpm workspace, electron-vite config, and all build scripts to work from src/ instead of packages/. The packages/ folder stays as read-only reference.
